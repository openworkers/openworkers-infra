services:
  postgres:
    ports:
      - 5432:5432
  openworkers-proxy:
    ports:
      - 80:80
      - 443:443
    entrypoint:
      [
        "/usr/sbin/nginx",
        "-c",
        "/etc/nginx/nginx.dev.conf",
        "-g",
        "daemon off;",
      ]
